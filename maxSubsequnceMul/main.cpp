#include <iostream>

using namespace std;

int main(void) {
 
    int size = 0, subseq = 0;
    cin >> size;
    int num[size];
    
    int matrix[size][size];
    
    
    
    for (int i = 0; i < size; i++) {
        cin >> num[i];
    }
    
    cin >> subseq;
    
    
    
    return 0;
}
